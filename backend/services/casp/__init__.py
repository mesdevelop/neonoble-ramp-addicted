"""CASP service package.

Provider adapters live here following an interface-first pattern so that
mock implementations can be swapped for live ones once API keys arrive
without touching consumer code (routes/services).
"""
